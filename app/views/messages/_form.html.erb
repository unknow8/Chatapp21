<%= form_with(model: message, local: true, class: 'form-inline') do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :room_id, value: room.id %>

  <div class="form-group">
    <%= form.text_field :content, placeholder: "Message", class: 'form-control' %>
  </div>

  <%# pressing enter without 'submit' button %>
  <input type="submit" hidden />
  <%# <input type="submit" style="height: 0px; width: 0px; border: none; padding: 0px;" hidefocus="true" /> %>
  <%# <%= form.submit "Add Message", class: "btn btn-dark"%>
<% end %>
